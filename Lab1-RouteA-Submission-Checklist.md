# Lab 1 — Route A (JavaScript + Node) Submission Checklist

Fill in the links and paste screenshots under each item.

- [ ] **GitHub profile link:** <https://github.com/YOUR_USERNAME>
- [ ] **Repo link:** <https://github.com/YOUR_USERNAME/hello-node-lab1>

## Screenshots to include (recommended order)
1. **Node and npm versions**
   - Terminal showing `node -v` and `npm -v`.
2. **Project runs locally**
   - Terminal showing `npm start` and the console output line:
     `Hello CSE 5234! Your Node.js environment is working.`
3. **VS Code open on the project**
   - Show the file tree and a terminal.
4. **Git configured (optional)**
   - `git config --global user.name` and `git config --global user.email`.
5. **GitHub repo page**
   - The repo you just published (code tab is fine).

> Save these into a PDF named `Lab1-DevSetup-YourName.pdf` and submit with your repo link.
